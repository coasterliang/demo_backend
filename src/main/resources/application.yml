server:
  port: 8848

spring:
  datasource:
    url: jdbc:mysql://172.16.146.168:3306/books
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      hibernate.ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.H2Dialect
  application:
    name: @artifactId@

